let canvas; // setup the canvas variable our work will show up on

// Runs exactly 1 time at the start of the program
function setup() {
    canvas = createCanvas(400, 400);
    background("#000000");
    ellipse(200, 200, 100, 100);
}

// Runs MANY time, at each FRAME
function draw() {
    // if (mouseIsPressed) {
    //     fill("#F2B92F55");
    //   } else {
    //     fill("#FFFFFF");
    //   }
    //   ellipse(mouseX, mouseY, 80, 80);
}
